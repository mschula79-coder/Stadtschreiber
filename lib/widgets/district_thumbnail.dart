import 'package:flutter/material.dart';

class DistrictThumbnail  extends StatelessWidget {
  const DistrictThumbnail({
    super.key
  });
  @override
  Widget build(BuildContext context) {
      return Text('hello');
  }

  
}

/* for (final d in districts) {
  final center = centroidOfDistrict(d);
  print("District ${d.name} center: lat=${center[0]}, lon=${center[1]}");
}
 */